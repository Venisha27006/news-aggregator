# Optional: point to API when not using Vite dev proxy (e.g. production build)
# VITE_API_URL=http://127.0.0.1:5000/api

# Optional: if the API runs on another port, match the Vite proxy target
# VITE_PROXY_TARGET=http://127.0.0.1:5001
